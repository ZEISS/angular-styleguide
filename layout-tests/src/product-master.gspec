# app-product-master spec

@objects
  header        app-product-master div.header-bg
    title       h1
    subtitle    h2
  content       app-product-master div.grid-container
    product-*   app-product

@set
  titleFontSize 65px
  subtitleFontSize 35px

== Header ==

  header:
    height 20 to 30 % of screen/height
    width 100 % of screen/width

  header.title:
    centered horizontally inside header
    css font-size is "${titleFontSize}

  header.subtitle:
    centered horizontally inside header
    css font-size is "${subtitleFontSize}"
    below header.title

== Content ==

  content:
    centered horizontally inside viewport
    width ~ 55 % of viewport/width

  @forEach [content.product-*] as product
    ${product}:
      height 230 to 280 px
      width 130 to 180 px
      component product.component.gspec

  @for [1 - 2] as index
    content.product-${index}:
      aligned horizontally all content.product-3

  @for [4 - 5] as index
    content.product-${index}:
      aligned horizontally all content.product-6
